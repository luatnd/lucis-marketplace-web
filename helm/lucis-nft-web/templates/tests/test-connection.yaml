apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lucis-nft-web.fullname" . }}-test-connection"
  labels:
    {{- include "lucis-nft-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lucis-nft-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
